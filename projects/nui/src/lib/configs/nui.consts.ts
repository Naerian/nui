import { NUIColor, NUISize, NUIVariant } from './common';

export const DEFAULT_SIZE: NUISize = 'md'; // Tamaño por defecto para los componentes
export const DEFAULT_COLOR: NUIColor = 'primary'; // Color por defecto para los componentes
export const DEFAULT_VARIANT: NUIVariant = 'solid'; // Variante por defecto para los componentes
