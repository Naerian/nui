import {
  aura,
  coffee,
  corporate,
  dopamine,
  emerald,
  minimal,
  neon,
  ocean,
  sunset,
  twilight,
  velvet,
  warm,
} from '../presets';
import { ThemePreset } from './theme.model';

/**
 * Array of available theme presets.
 * Each preset includes a name, light and dark color schemes, and an optional set of grays.
 */
export const NUI_PRESETS: ThemePreset[] = [
  aura,
  coffee,
  corporate,
  dopamine,
  emerald,
  minimal,
  neon,
  ocean,
  sunset,
  twilight,
  velvet,
  warm,
];
